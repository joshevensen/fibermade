name = "React Router"
roles = ["frontend", "backend"]
webhooks_path = "/webhooks/app/uninstalled"

[commands]
predev = "npx prisma generate"
dev = "npx prisma migrate deploy && NODE_OPTIONS='--import ./instrument.server.mjs' npm exec react-router dev"
